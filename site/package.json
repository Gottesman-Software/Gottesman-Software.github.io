{
  "name": "gottesman-software-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174",
    "dev:lidmas-live": "VITE_ENABLE_LIDMAS_LIVE_ROUTE=1 VITE_LIDMAS_LIVE_ORIGIN=http://127.0.0.1:5173 vite --host 127.0.0.1 --port 5174",
    "build": "npm --prefix studio/lidmas run build:studio && vite build && node scripts/copy-spa-routes.mjs",
    "build:public-api": "npm --prefix studio/lidmas run build:studio:public-api && vite build && node scripts/copy-spa-routes.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 4174"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.10",
    "typescript": "^5.6.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {}
}
